# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET     /                                   controllers.Application.list()
GET     /add-place                          controllers.Application.addPlace()
GET     /get-picture-of-place/:id           controllers.Application.getPictureOfPlace(id: Long)
GET     /place/:place                       controllers.Application.details(place: models.Place)
POST    /                                   controllers.Application.upload()

# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file               controllers.Assets.at(path="/public", file)
